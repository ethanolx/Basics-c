#ifndef MAX_H
#define MAX_H

#include "../utils.h"

long find_max(const long[], long, long);

bool max_handler(const char[][MAX_LINE_LENGTH], const int,
                 const char[][MAX_LINE_LENGTH], char[]);

#endif
