#ifndef PADOVAN_H
#define PADOVAN_H

#include "../utils.h"

long long padovan(bool[], long long[], const long);

bool padovan_handler(const char[][MAX_LINE_LENGTH], const int,
                     const char[][MAX_LINE_LENGTH], char[]);

#endif
